import { Container, Loader, Card } from './style';

function LoaderCard() {
  return (
    <Container>
      <Card>
        <Loader />
      </Card>
    </Container>
  );
}

export default LoaderCard;
